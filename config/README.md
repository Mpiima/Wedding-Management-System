# Application configuration

This directory is reserved for application-level configuration that is not part of the reusable Core:

- Environment and deployment configuration.
- Module-specific configuration.
- Feature flags or per-project settings.

Core database configuration lives in `core/config/database.php`.

